<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ktds.crmai.service.impl.AccountMapper">

	<select id="selectAllAccountList" resultType="account">
		SELECT 
		    ADM_ID AS "adm_id", 
			ADM_PW AS "adm_pw",
			ADM_NAME AS "adm_name",
			ADM_EMAIL AS "adm_email",
			ADM_CDATE as "adm_cdate",
			ADM_TYPE as "adm_type"

		FROM  
			AI_ADMINUSER
 
	</select>
	
	<select id="selectAccountList" resultType="account">
		SELECT
		     ADM_ID AS "adm_id", 
			ADM_PW AS "adm_pw"
	
		FROM 
		    AI_ADMINUSER
		<where>
			<if test="adm_id != null and adm_id != '' ">
				adm_id like '%' || #{adm_id} || '%'
			</if>
			
			<if test="adm_pw != null and adm_pw != '' ">
				AND type adm_pw '%' || #{adm_pw} || '%'
			</if>
		</where>

    </select>
    
    <select id="selectAccountDetail" resultType="account">
			SELECT 
		    ADM_ID AS "adm_id", 
			ADM_PW AS "adm_pw",
			ADM_NAME AS "adm_name",
			ADM_EMAIL as "adm_email",
			ADM_CDATE as "adm_cdate",
			ADM_TYPE as "adm_type"

		FROM  
			AI_ADMINUSER
		WHERE
			adm_id =  #{adm_id}
    </select>
    <select id="selectCheckPK" resultType="int">
			SELECT count(*)
		FROM  
			AI_ADMINUSER
		WHERE
			adm_id =  #{adm_id}
    </select>
    
    <insert id="insertAccount">
    	INSERT INTO AI_ADMINUSER (ADM_ID, ADM_PW, ADM_NAME, ADM_CDATE, ADM_UDATE, ADM_EMAIL, ADM_ROLE, ADM_TYPE, ADM_LOGIN)
      	VALUES (#{adm_id}, #{adm_pw}, #{adm_name}, sysdate, sysdate, #{adm_email},1,#{adm_type},0)
    </insert>
	
	<update id="updateAccount">
	update AI_ADMINUSER set ADM_PW=#{adm_pw}, ADM_NAME=#{adm_name}, ADM_EMAIL=#{adm_email}, ADM_UDATE=sysdate, ADM_TYPE=#{adm_type} where ADM_ID=#{adm_id}
	</update>

    
    <delete id="deleteAccount">
    	DELETE FROM AI_ADMINUSER 
    	WHERE 
    		adm_id =  #{adm_id}
    </delete>
    
    
    <select id="selectAccountPage" resultType="account">
		select *
		from (
		select <![CDATA[ rownum as rnum ]]>
		, A.ADM_ID, A.ADM_PW, A.ADM_NAME, A.ADM_EMAIL, A.ADM_CDATE, A.ADM_TYPE
		from
		(
		select
			ADM_ID, 
			ADM_PW,
			ADM_NAME,
			ADM_EMAIL,
			ADM_CDATE,
			ADM_TYPE
		from AI_ADMINUSER
		order by ADM_CDATE desc
		) A
		where <![CDATA[ rownum <= #{page_end} ]]>
		) B
		where b.rnum >= #{page_st}
	</select>

	<select id="selectAccountPageNum" resultType="int">
		select count(*) as
		totalCount from AI_ADMINUSER
	</select>
	
	
</mapper>	